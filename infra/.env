# PostgreSQL Configuration
# These defaults match the values hardcoded in each service's application.properties
POSTGRES_USER=admin
POSTGRES_PASSWORD=admin

# MongoDB Configuration (no auth by default – matches service application.properties)
MONGO_USER=
MONGO_PASSWORD=

# Redis Configuration
REDIS_PORT=6379

# Kafka Configuration
KAFKA_PORT=9092
